name: Build and Release

on:
  workflow_dispatch:
    inputs:
      major:
        description: "Major version"
        required: true
        default: "0"
      minor:
        description: "Minor version"
        required: true
        default: "0"
  push:
    tags:
    - "v*"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v6.0
    - name: Computer version
      id: version
      run: |
        VERSION=v${{ github.event.inputs.major }}.${{ github.event.inputs.minor }}.${{ github.sha }}
        echo "VERSION=$VERSION" >> $GITHUB_ENV
        echo "version=$VERSION" >> $GITHUB_OUTPUT
    - name: Install Compilers
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential mingw-w64
    - name: Setup Dependencies
      run: ./setup.sh windows
    - name: make Windows
      run: make PLATFORM=windows
    - name: Package Windows
      run: zip -r fantasia-windows_x86_64.zip resources -j bin/*
    - name: Create tag
      run: |
        git tag $VERSION
        git push origin $VERSION
    - name: Create Release
      uses: softprops/action-gh-release@v3.0
      with:
        tag_name: ${{ steps.version.outputs.version }}
        files: |
          fantasia-windows_x86_64.zip
